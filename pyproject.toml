[project]
name = "splitter"
version = "1.0.0"
description = "PDF to HTML converter with user management"
requires-python = ">=3.11,<3.12"

[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[tool.poetry.dependencies]
python = "^3.11"
